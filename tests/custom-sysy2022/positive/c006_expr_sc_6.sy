int g = 0;
int tick() {
  g = g + 1;
  return g;
}
int main() {
  int a = 8;
  int b = 4;
  int c = a + b * 2 - a % b;
  int x = 0;
  if ((a < b) && tick()) x = x + 10;
  if ((a > b) || tick()) x = x + 20;
  putint(c + x + g);
  putch(10);
  return 0;
}
