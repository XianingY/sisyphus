int main() {
  int n = 21;
  float a[64];
  int i = 0;
  while (i < n) {
    a[i] = ((i % 5) + 2.0) / 4.0;
    i = i + 1;
  }

  int s = 0;
  i = 0;
  while (i < n) {
    if (i % 3 == 0) s = s + a[i] * 4.0;
    else s = s + a[i] * 2.0;
    i = i + 1;
  }

  putint(s);
  putch(10);
  return 0;
}
